{
  "name": "claude-code-sessions",
  "version": "0.1.0",
  "description": "Session intelligence for Claude Code — search, analyze, diff, and resume past sessions",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "bunx tsc --noEmit",
    "lint": "bunx biome check ./lib ./ui/server.ts ./tests",
    "lint:fix": "bunx biome check --write ./lib ./ui/server.ts ./tests",
    "ui": "bun run ui/server.ts",
    "ui:open": "bun run ui/server.ts & sleep 1 && open http://localhost:3000"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.11",
    "@types/bun": "1.3.12"
  }
}
